import React from 'react';
import Slider from '../ShareableComponents/Slider';

const HomePage = () => {
    return (
        <div>
            <Slider/>
            
        </div>
    );
};

export default HomePage;